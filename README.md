# FORA

Final year Project:

It is an mobile application developed using flutter. The admin panel is developed using React JS which uses MUI Components for faster frontend development. For the backend and database, Django framework and PostgresSQL is used.
